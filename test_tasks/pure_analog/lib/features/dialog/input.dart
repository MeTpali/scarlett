import 'package:flutter/material.dart';

class PureInput extends StatelessWidget {
  const PureInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
