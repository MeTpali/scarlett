import 'package:flutter/material.dart';

class DefaultLocaleTile extends StatelessWidget {
  const DefaultLocaleTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
