name: testing
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  extra_pedantic: ^4.0.0
  topg:
    path: ../features/topg
  i18n:
    path: ../features/i18n

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
